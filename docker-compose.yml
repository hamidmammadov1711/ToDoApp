version: '3.8'

services:
  web:
    build: .
    container_name: todoapp_web
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:///./todoapp.db
    restart: unless-stopped
    volumes:
      - todoapp_data:/app

volumes:
  todoapp_data:
